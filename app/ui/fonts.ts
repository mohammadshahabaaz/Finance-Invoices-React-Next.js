import { Inter,Lusitana } from 'next/font/google';
 
// Define your fonts
export const inter = Inter({
    weight: ['400', '700'],
    subsets: ['latin'] // Specify the subsets for the Inter font
  });
  
  export const lusitana = Lusitana({
    weight: ['400', '700'],
    subsets: ['latin'] // Specify the subsets for the Lusitana font
  });